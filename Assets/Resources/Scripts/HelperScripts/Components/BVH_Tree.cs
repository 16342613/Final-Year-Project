using System;
public class BVH_Tree
{
    BVH_Node root;

    public BVH_Tree(BVH_Node root)
    {
        this.root = root;
    }

    public void TraverseTree()
    {
        //get raycast from point and continue it along the tree to see its ending box

        // Activate colliders in the last BVH
    }
}
